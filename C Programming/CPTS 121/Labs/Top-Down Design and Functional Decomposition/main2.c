//Uncomment this file to compile 2nd problem

/*#include "duration.h"

int main() {
	double t = theta();
	double d = distance();
	double v = velocity();
	double time = (d) / (v * cos(t));
	printf("time = (%lf) / (%lf * cos(%lf)) = %lf\n", d, v, t, time);
	printf("height = %lf * sin(%lf) * %lf - ((%.2f * %lf^2) / 2) = %lf\n", v, t, time, G, time, v * sin(t) * time - ((G * pow(time, 2)) / 2));
}*/